using System;
${nav_usings}

namespace ${namespace}
{
    public class ${entity_name}WithNavigationProperties
    {
        public ${entity_name} ${entity_name} { get; set; } = null!;
        ${with_nav_properties}
    }
}
